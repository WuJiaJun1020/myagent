# 智能面试系统项目计划书（V0.1）

## 0. 当前实施基线（客户端 0.5.0）

本节记录已经确认并开始落地的方案；与下方早期设想冲突时，以本节为准。

- 面试系统融入现有 Electron + React 客户端，不再另建 Tauri 壳。
- 面试业务模块与 Pi Agent 解耦：Pi 是可替换的推理运行时，不是面试数据的事实来源。
- 面试业务记忆使用独立 SQLite 数据库，保存岗位、候选人、问答、评分、证据与报告。
- 向量检索使用嵌入式 LanceDB；向量数据是可重建索引，不能替代 SQLite 中的正式记录。
- 普通 Pi 会话、Pi Memory 与面试记录不共用存储；未来通过带 `interviewId` 的上下文组装器受控互通。
- 当前最小版本只实现本地单用户文本面试：粘贴 JD 和简历、创建草稿、逐题问答、中断恢复和证据化报告。
- PDF/DOCX、语音、岗位采集、多用户服务端和训练趋势均放在后续迭代。

首个纵向切片从客户端 0.5.0 开始：提供独立“智能面试”入口、SQLite schema 与迁移、面试草稿持久化、LanceDB 向量存储端口及真实检索测试。该阶段不调用 Pi，也不会向普通会话列表写入测试会话。

## 1. 项目概述

### 1.1 项目名称

暂定名：**InterviewPilot - 面向互联网技术岗位的证据驱动型智能面试系统**

### 1.2 项目目标

开发一套可独立运行的智能面试系统。系统持续收集公开岗位信息，解析候选人简历，建立“岗位要求—个人经历—知识点—面试题”之间的关联，并据此生成和执行动态模拟面试。面试结束后，系统提供可追溯的评分依据、能力缺口和训练建议。

项目首先面向互联网技术岗位，优先支持后端开发、AI 应用开发和 Agent 开发岗位。首版使用 Web 技术实现完整界面，并通过 Tauri 封装为桌面客户端。

### 1.3 核心价值

普通题库只能回答“有哪些题”，通用聊天机器人只能提供一轮泛化问答。本项目重点解决三个具体问题：

1. **岗位针对性不足**：将实时岗位要求转化为技能画像，再与候选人简历和题库匹配。
2. **模拟面试流程僵化**：根据回答质量、证据充分度和暴露出的薄弱点动态选择追问方向。
3. **评分缺乏可信依据**：评分绑定岗位要求、评分量表、候选人原回答及检索到的参考资料，允许用户追溯和复核。

### 1.4 简历描述建议

> 独立设计并实现面向互联网技术岗位的智能面试系统，使用 FastAPI、React/TypeScript、PostgreSQL/pgvector 构建岗位采集、简历解析、混合检索和面试状态管理模块；基于可配置 Agent 工作流实现按回答表现动态追问，通过 MCP 暴露岗位与知识库工具，并以证据引用和多维量表降低大模型评分的不透明性。

该描述应在项目完成后根据真实压测结果、数据规模和评估指标补充数字，禁止预先虚构。

## 2. 目标用户与使用场景

### 2.1 目标用户

- 准备互联网技术岗校招或社招的开发者。
- 希望按照指定公司、岗位或技术方向进行针对性训练的求职者。
- 需要反复训练项目深挖、技术基础和场景设计问题的初中级工程师。

### 2.2 核心用户流程

1. 用户导入 PDF/DOCX 简历，系统提取教育、经历、项目、技能及可验证事实。
2. 用户从岗位库选择一个或多个目标岗位，也可以粘贴岗位描述。
3. 系统生成岗位技能画像，并展示简历与岗位之间的匹配证据和能力缺口。
4. 用户选择面试时长、难度、面试类型和是否启用语音，创建面试。
5. 面试官 Agent 按计划提问，根据回答动态追问、切换知识点或调整难度。
6. 系统生成结构化报告，用户查看原始问答、评分依据、知识引用和改进任务。
7. 用户针对薄弱能力创建训练计划，再次面试后比较前后变化。

## 3. 产品创新点

### 3.1 创新点一：岗位—简历—题目“证据图谱”

系统不只计算一个模糊的简历匹配分，而是保存以下关系：

```text
岗位要求 -> 标准技能/知识点 -> 简历证据 -> 面试问题 -> 回答证据 -> 评分项
```

例如，岗位要求“熟悉异步任务和消息队列”，系统可关联到简历中的 FastAPI 项目、对应知识点、追问题目以及回答中提到的幂等性设计。用户可以查看每个结论来自哪里。

首版使用 PostgreSQL 关系表实现，不引入图数据库；当关系规模和查询复杂度确实需要时，再评估 Neo4j。

### 3.2 创新点二：基于不确定度的动态追问

系统不会仅按预生成题目列表顺序提问。每轮回答后评估：

- 正确性：关键结论是否正确。
- 完整性：是否覆盖评分量表中的关键点。
- 证据性：是否用真实经历、数据或设计决策支撑观点。
- 一致性：当前回答是否与简历和之前回答冲突。
- 不确定度：现有证据是否足以判断该项能力。

当不确定度较高时，Agent 优先追问；证据充分时则进入下一个能力点。该设计既能体现 Agent 的决策能力，也能避免“为了追问而追问”。

### 3.3 创新点三：可审计的评分与反幻觉机制

每项评分必须绑定以下内容：

- 评分量表及岗位权重。
- 用户原始回答片段。
- 题库或知识库引用。
- 模型给出的评分理由与置信度。
- 规则校验结果，例如是否存在无引用结论、分数与理由矛盾。

关键评分采用“结构化输出 + 规则校验 + 必要时二次复核”，并明确区分事实错误、表达不足和缺少证据。系统不声称预测真实录用结果。

### 3.4 创新点四：训练闭环而非一次性报告

报告将能力缺口转化为可执行任务，例如补充项目指标、复习某个知识点、完成一轮限时回答。后续面试复用同一能力维度，展示趋势而非只保存历史总分。

## 4. 范围设计

### 4.1 MVP 必须完成

- 用户本地账号或单用户模式。
- PDF/DOCX 简历上传、文本提取和结构化解析。
- 岗位信息手动录入、URL 导入，以及至少两个合规公开来源的定时采集。
- 岗位去重、版本记录、技能标签提取和筛选。
- 面试题库导入、切分、标签化、向量化与检索。
- 简历与岗位匹配分析。
- 文本模拟面试、动态追问和中断恢复。
- 多维评分、引用展示和面试报告。
- Web 界面及 Tauri 桌面打包。
- 基础日志、错误处理、数据删除和隐私说明。

### 4.2 第二阶段

- 语音识别、语音合成及计时模式。
- 多模型切换和本地模型支持。
- 岗位变化趋势与公司维度统计。
- 训练计划、复习提醒和跨场次能力趋势。
- OCR 简历支持。
- 更完善的 MCP 工具生态和第三方客户端接入。

### 4.3 暂不纳入

- 自动海投、代替用户与招聘方沟通。
- 绕过验证码、登录限制或网站反爬机制。
- 视频表情、情绪或人格判断。这类结论偏差大且容易造成不可信展示。
- 微服务、Kubernetes、复杂多租户权限。它们会显著增加工作量，却不能增强首版核心价值。

## 5. 技术选型

| 层级 | 推荐技术 | 选择理由 |
| --- | --- | --- |
| 桌面与 Web UI | React + TypeScript + Vite | 学习成本可控，生态完整，同一套页面可用于 Web 和桌面端 |
| 桌面壳 | Tauri 2 | 安装包较小，可复用 Web 前端；首版也可先完成 Web 再封装 |
| 后端 API | Python 3.12 + FastAPI + Pydantic | 与现有学习栈一致，适合异步 API 和结构化模型输出 |
| Agent 编排 | LangGraph 或显式状态机 | 面试是有状态、可恢复的流程，不适合用单次链式调用表示 |
| ORM/迁移 | SQLAlchemy 2 + Alembic | 类型和迁移能力成熟 |
| 主数据库 | PostgreSQL 16 | 同时承载业务数据、全文检索与结构化查询 |
| 向量检索 | pgvector | 降低基础设施复杂度，足够支持作品集规模 |
| 异步任务 | 首版 FastAPI BackgroundTasks；后续 Celery/RQ + Redis | 先控制复杂度，任务量增长后再拆分 |
| 文档解析 | PyMuPDF、python-docx | 覆盖 MVP 的 PDF/DOCX 简历 |
| 数据采集 | httpx + selectolax/BeautifulSoup，必要时 Playwright | 优先使用公开 API 和静态页面，动态页面才用浏览器 |
| 测试 | pytest、Testcontainers、Vitest、Playwright | 覆盖单元、数据库集成与关键用户流程 |
| 可观测性 | structlog + OpenTelemetry（第二阶段） | 记录模型调用、检索结果、耗时和错误链路 |

建议采用“模块化单体”而非一开始拆微服务。代码按领域分层，未来可以单独拆出采集任务或模型服务。

## 6. 系统架构

```text
React/TypeScript UI  <->  FastAPI Application
       |                        |
   Tauri shell          Application Services
                                |
              +-----------------+------------------+
              |                 |                  |
        Interview Engine   Retrieval Service   Ingestion Workers
              |                 |                  |
              +----------- PostgreSQL ------------+
                              + pgvector
                                |
                        LLM / Embedding Provider

MCP Client <-> MCP Server Adapter <-> Job / Resume / Knowledge Services
```

### 6.1 后端领域模块

- `auth`：用户与会话；单用户版本可简化，但保留用户边界。
- `resumes`：文件管理、文本提取、结构化解析、证据定位。
- `jobs`：岗位采集、标准化、去重、版本和技能画像。
- `knowledge`：题库、知识文档、切分、索引和检索。
- `interviews`：会话状态、提问决策、回答分析、评分和恢复。
- `reports`：报告聚合、能力趋势和导出。
- `llm`：模型适配、结构化输出、重试、预算和调用日志。
- `mcp`：将受控业务能力暴露为工具。

### 6.2 Agent 工作流

```text
创建会话
  -> 分析岗位画像与简历证据
  -> 生成覆盖计划
  -> 检索候选问题和评分量表
  -> 提问
  -> 分析回答并提取证据
  -> 决策：追问 / 提高难度 / 切换主题 / 结束
  -> 汇总并进行评分一致性检查
  -> 生成训练报告
```

工作流状态必须持久化。每个节点输入输出使用 Pydantic 模型约束；模型只负责语义判断，业务代码负责状态转换、轮次上限、超时、权限和写库。

## 7. RAG 设计

### 7.1 知识来源

- 自建并人工审核的技术题库。
- 官方文档、公开技术文章和允许使用的学习材料。
- 已采集岗位描述。
- 用户简历及其结构化证据。
- 每场面试的历史问答，仅在当前用户数据边界内使用。

所有来源保存标题、URL、发布时间、采集时间、许可证或来源类型。过期岗位与新版岗位分开存储，不直接覆盖。

### 7.2 入库流程

1. 加载原始文档并计算内容哈希。
2. 清洗导航、广告和重复段落。
3. 按标题和语义段落切分，保留父文档关系。
4. 提取岗位、技能、难度、题型等元数据。
5. 生成向量并写入 `document_chunks`。
6. 抽样或规则检查切分质量。

### 7.3 检索流程

采用混合检索而非单纯向量相似度：

1. 根据目标岗位、当前能力点和面试历史生成检索查询。
2. 使用 PostgreSQL 全文检索和 pgvector 分别召回。
3. 通过 RRF 合并结果，按岗位、难度、来源和时效过滤。
4. 使用轻量重排模型或 LLM 对少量候选重排。
5. 将最终引用及 chunk ID 传入提问或评分节点。

### 7.4 RAG 评估指标

- `Hit@K`：标注相关片段是否进入前 K 条。
- `MRR` 或 `nDCG@K`：相关结果排序质量。
- 引用正确率：报告引用是否真的支持对应结论。
- 忠实度：回答或评分是否只使用给定上下文中的事实。
- 延迟和单场面试模型成本。

准备 30 至 50 个带标准相关文档的评估查询，作为回归数据集。简历、岗位和题库分别覆盖。

## 8. MCP 设计

MCP 的价值应是“标准化工具边界”，而不是为了简历强行增加一层调用。

### 8.1 MCP Server 提供的工具

- `search_jobs(filters)`：按公司、城市、技能、时间检索岗位。
- `get_job_profile(job_id)`：返回岗位画像和来源证据。
- `get_resume_profile(resume_id)`：返回脱敏后的简历结构及证据定位。
- `search_interview_knowledge(query, filters)`：检索题目、知识点和评分量表。
- `get_interview_summary(session_id)`：读取已授权会话摘要。

### 8.2 MCP Resources

- `job://{job_id}`：岗位原文与版本。
- `resume://{resume_id}/profile`：结构化简历画像。
- `interview://{session_id}/report`：面试报告。

### 8.3 安全边界

- 工具输入使用 JSON Schema 校验。
- 默认只读；写操作不在首版 MCP 中开放。
- 每次调用校验用户归属并记录审计日志。
- 简历原始文件不通过 MCP 暴露，默认返回脱敏结构。
- MCP 适配层调用既有应用服务，不复制业务逻辑。

演示时可以使用支持 MCP 的客户端查询岗位库或面试报告，证明系统能力可以被外部 Agent 复用。

## 9. 数据库初步设计

| 表 | 关键字段 | 用途 |
| --- | --- | --- |
| `users` | id, email, settings | 用户与模型配置 |
| `resumes` | id, user_id, file_hash, raw_path, parsed_status | 简历文件及处理状态 |
| `resume_evidence` | id, resume_id, type, content, page, offsets | 可定位的简历事实 |
| `job_sources` | id, name, base_url, policy | 岗位来源配置 |
| `jobs` | id, source_id, external_id, company, title, location | 标准化岗位 |
| `job_versions` | id, job_id, content_hash, raw_text, published_at | 岗位版本与原文 |
| `skills` | id, canonical_name, aliases, category | 标准技能字典 |
| `job_skills` | job_id, skill_id, weight, evidence | 岗位技能与原文证据 |
| `resume_skills` | resume_id, skill_id, confidence, evidence_id | 简历技能与证据 |
| `documents` | id, source_type, title, url, content_hash | RAG 父文档 |
| `document_chunks` | id, document_id, content, metadata, embedding | 检索片段 |
| `questions` | id, type, difficulty, rubric, status | 结构化面试题与量表 |
| `question_skills` | question_id, skill_id, weight | 题目覆盖能力 |
| `interview_sessions` | id, user_id, job_id, state, config | 面试状态与配置 |
| `interview_turns` | id, session_id, role, content, decision | 每轮问答及决策 |
| `evaluations` | id, turn_id, dimension, score, evidence, confidence | 细粒度评分 |
| `reports` | id, session_id, summary, version | 报告与生成版本 |
| `llm_calls` | id, purpose, model, tokens, latency, status | 成本与故障分析 |

向量列应保存 embedding 模型和版本。更换模型时建立新索引版本，避免不同向量空间混用。

## 10. 页面与交互设计

### 10.1 主要页面

- **工作台**：待处理简历、目标岗位、最近面试和训练任务。
- **岗位库**：表格筛选、岗位详情、来源与历史版本对比。
- **简历分析**：原文证据、结构化经历、技能匹配和待确认项。
- **面试配置**：岗位、时长、类型、难度和重点能力。
- **面试室**：问题区、回答输入、计时、结束和断线恢复。
- **面试报告**：能力雷达/条形图、逐题评分、引用、矛盾点和训练建议。
- **知识库**：文档、题目、处理状态、标签与检索测试台。
- **设置**：模型、Embedding、数据源、隐私和数据删除。

### 10.2 交互原则

- 面试时界面保持克制，避免展示会干扰作答的参考答案。
- 所有 AI 解析结果均允许用户确认或修改。
- 长任务展示明确状态：排队、解析、索引、失败及重试。
- 报告中的结论可展开查看证据，不用单一总分掩盖细节。
- 桌面端密钥优先保存在操作系统安全存储中，不写入数据库明文。

## 11. API 初步设计

```text
POST   /api/v1/resumes
GET    /api/v1/resumes/{id}
POST   /api/v1/resumes/{id}/analyze

GET    /api/v1/jobs
POST   /api/v1/jobs/import
GET    /api/v1/jobs/{id}
POST   /api/v1/jobs/{id}/match/{resume_id}

POST   /api/v1/knowledge/documents
GET    /api/v1/knowledge/search

POST   /api/v1/interviews
GET    /api/v1/interviews/{id}
POST   /api/v1/interviews/{id}/answers
POST   /api/v1/interviews/{id}/finish
GET    /api/v1/interviews/{id}/report
```

回答接口首版可使用普通 HTTP；需要流式显示时使用 SSE。只有在明确需要双向实时语音时再引入 WebSocket。

## 12. 开发阶段与交付物

按每周约 15 至 20 小时估算，完整 MVP 约需 10 至 12 周。如果时间不足，应减少数据源和语音功能，不削弱主流程质量。

### 阶段 0：需求与工程骨架（第 1 周）

- 确定目标岗位、数据来源、模型提供商和隐私策略。
- 建立 monorepo、后端、前端、数据库、迁移和 CI。
- 定义领域模型、接口约定和种子数据。

验收：本地一条命令启动依赖，前后端健康检查通过，CI 执行格式检查和基础测试。

### 阶段 1：岗位与简历数据管线（第 2 至 3 周）

- 完成简历上传、抽取、结构化解析和证据定位。
- 完成岗位手动/URL 导入、采集适配器、去重和版本。
- 完成岗位及简历技能标准化与匹配页面。

验收：使用至少 10 份脱敏简历和 100 条岗位数据，重复采集不会产生重复岗位；技能结论可以定位到原文。

### 阶段 2：题库与 RAG（第 4 至 5 周）

- 完成知识文档和题库的数据模型、入库与管理页面。
- 完成混合检索、过滤、重排和引用返回。
- 建立首版离线检索评估集。

验收：固定评估集达到事先约定的 `Hit@5`，并能在检索测试台查看排序过程和引用片段。

### 阶段 3：自适应面试 Agent（第 6 至 7 周）

- 实现状态图、覆盖计划、问题选择和追问策略。
- 实现结构化回答分析、评分和中断恢复。
- 建立最大轮次、失败降级、幂等和模型超时机制。

验收：完成一场 20 至 30 分钟的文本面试；刷新或重启服务后可以继续；同一能力点不会无限追问。

### 阶段 4：报告、评估与训练闭环（第 8 至 9 周）

- 实现逐题和能力维度报告、证据引用和置信度。
- 实现评分一致性检查和训练建议。
- 建立人工评分对照集，评估模型评分稳定性。

验收：报告中的每个关键结论可追溯；相同输入多次评估的波动处于约定范围；无证据时明确显示“不足以判断”。

### 阶段 5：MCP、桌面打包与打磨（第 10 至 12 周）

- 实现只读 MCP 工具和资源，接入一个外部 MCP 客户端演示。
- 完成 Tauri 打包、密钥存储和桌面端升级策略说明。
- 补充端到端测试、演示数据、项目文档和演示视频。

验收：新环境可以按 README 启动；核心演示流程无手工改库；敏感数据不会出现在日志和代码仓库中。

## 13. 测试与质量保障

### 13.1 自动化测试

- 单元测试：技能标准化、岗位去重、分数聚合、状态转换。
- 属性/边界测试：轮次上限、空检索结果、重复回答、超长文本。
- 集成测试：PostgreSQL/pgvector、文档入库、会话恢复。
- 契约测试：LLM 结构化响应和 MCP 工具 Schema。
- 端到端测试：上传简历 -> 选择岗位 -> 完成面试 -> 查看报告。

### 13.2 AI 功能评估

- 简历字段抽取：字段级 Precision/Recall/F1。
- 技能匹配：人工标注样本上的 Precision@K。
- 检索：Hit@K、MRR/nDCG、引用正确率。
- 评分：与两名人工评分者的相关性或加权 Kappa。
- 稳定性：同一输入多次运行的评分方差。
- 性能：P50/P95 延迟、每场 Token 用量和费用。

测试数据必须脱敏。模型输出快照不应作为唯一断言，优先验证 Schema、约束和关键事实。

## 14. 风险与应对

| 风险 | 应对策略 |
| --- | --- |
| 招聘网站反爬或条款限制 | 优先官方公开接口、RSS、允许抓取页面和用户主动导入；记录来源策略，不绕过验证码 |
| 岗位数据快速过期 | 保存发布时间、采集时间、版本和失效状态，默认优先近期岗位 |
| 简历隐私泄露 | 本地优先、用户隔离、日志脱敏、可删除；调用云模型前明确提示 |
| 大模型评分不稳定 | 固定量表、低温度、结构化输出、引用约束、规则校验和人工对照集 |
| RAG 召回不准 | 混合检索、元数据过滤、评估集驱动调优，保留检索调试信息 |
| Agent 失控或循环 | 明确状态机、轮次和追问上限，模型不直接控制关键业务操作 |
| 项目范围过大 | 先做文本闭环和两个岗位方向；语音、趋势和多模型后置 |
| 前端学习成本 | 先完成核心 React 页面，再引入 Tauri，不同时维护两套 UI |

## 15. 项目展示与面试讲解

### 15.1 五分钟演示路径

1. 导入一份脱敏简历，展示结构化结果及原文证据。
2. 从岗位库选择目标职位，展示岗位技能画像和能力缺口。
3. 开始面试，故意给出一个不完整回答，展示系统如何基于不确定度追问。
4. 完成短面试，查看有引用的多维报告及训练任务。
5. 从 MCP 客户端查询同一岗位画像，展示标准化工具能力。

### 15.2 面试时重点说明

- 为什么采用模块化单体和 PostgreSQL/pgvector。
- 为什么面试流程用状态机而不是自由运行的多 Agent。
- 如何评估 RAG，而不只凭肉眼判断结果。
- 如何防止评分幻觉、数据泄露和无限追问。
- MCP 在项目中解决了什么互操作问题。
- 哪些结果由模型判断，哪些约束由确定性代码负责。

## 16. 代码实施原则

- 后端业务逻辑不能直接散落在路由或 Prompt 中。
- 所有模型响应使用版本化的 Pydantic Schema。
- Prompt 作为可测试、可追踪的资源管理，记录版本。
- 外部模型、Embedding 和岗位数据源均通过适配器隔离。
- 数据库变更必须使用 Alembic 迁移。
- 每个开发任务必须包含验收标准和必要测试。
- 优先提交纵向可运行功能，避免先创建大量空目录和占位类。
- README 必须包含架构、启动方式、配置、安全说明和演示流程。

## 17. 后续交给 Codex 的建议任务顺序

1. 创建 monorepo 工程骨架及开发环境。
2. 实现 PostgreSQL 数据模型和首批迁移。
3. 实现简历上传、解析、证据定位及测试。
4. 实现岗位导入、去重、版本管理及测试。
5. 实现技能字典、岗位画像和简历匹配。
6. 实现知识库入库、混合检索和评估脚本。
7. 实现面试状态机、问题选择和回答分析。
8. 实现报告与评分一致性检查。
9. 实现核心 React 页面和端到端流程。
10. 实现 MCP Server、Tauri 封装和发布文档。

每一步都应先读取本计划及仓库现状，再给出小范围实施方案；不得一次生成整个系统。

## 18. 当前假设与待确认项

V0.1 暂时采用以下假设：

- 首要目标是求职作品集，兼顾真实可用性，不以短期商业化为目标。
- 第一批用户是中文互联网技术岗求职者，界面和面试以中文为主。
- MVP 聚焦后端开发、AI 应用开发和 Agent 开发岗位。
- 可以使用云端大模型 API，但需要控制调用费用并保护简历隐私。
- 开发者每周可投入约 15 至 20 小时，目标周期为 10 至 12 周。
- 初期单人开发，部署以本地演示为主，可补充一个在线体验环境。

需要确认的问题：

1. 你的求职方向和目标岗位是什么，最希望该项目向面试官证明哪三项能力？
2. 你希望 MVP 必须支持语音面试，还是先把文本面试质量做好后再加语音？
3. 你是否能使用付费大模型 API？是否要求敏感简历全程不离开本机？
4. 你每周可投入多少时间，希望最晚何时完成可演示版本？
5. 你希望项目主要作为本地桌面应用展示，还是还要部署公开 Web Demo？
6. 你目前能合法、稳定获取哪些岗位来源或题库资料？

---

文档状态：V0.1，等待需求确认。确认后应补充：明确的 MVP 优先级、模型与部署方案、数据来源清单、页面原型、项目目录结构及逐迭代任务卡。
